# 计算机网络面试题库

## HTTP

- 1. HTTP 和 HTTPS 的区别是什么？✅ 2026-05-18 14:30 45分

  **答：** 增加 HTTP 证书，中间使用了 TLS 进行加密，是一种非对称加密。

  **完整答案：** HTTP 和 HTTPS 的区别从四个维度展开：

  **1. 协议层与端口**：HTTP 是明文传输的应用层协议，默认端口 80；HTTPS = HTTP + TLS/SSL，在 TCP 和 HTTP 之间加了一层 TLS，默认端口 443。

  **2. 安全性（HTTPS 解决的三大问题）**：
  - 机密性：混合加密（非对称 + 对称）。握手阶段用非对称加密（RSA/ECDHE）协商出对称密钥，通信阶段用对称加密（AES-GCM、ChaCha20）传输数据
  - 完整性：通过 MAC / HMAC / AEAD（如 AES-GCM）防止数据被篡改
  - 身份认证：通过 CA 颁发的数字证书验证服务端身份，防止中间人攻击。证书包含公钥、域名、有效期、CA 签名，浏览器通过预置根证书验证证书链

  **3. 性能**：HTTPS 多了 TLS 握手开销（TLS 1.2 需 2-RTT，TLS 1.3 优化到 1-RTT，0-RTT 会话恢复）；加密解密 CPU 开销现代硬件几乎可忽略（AES-NI 指令集）；HTTP/2 必须基于 HTTPS，反而能享受多路复用、头部压缩带来的性能提升。

  **4. 其他**：HTTPS 是 SEO 排名因子；浏览器对 HTTP 站点标"不安全"；Service Worker、Geolocation、Camera 等 Web API 必须 HTTPS。

  **延伸 A：** 既然 HTTPS 用了非对称加密保证安全，为什么实际通信时还要切换到对称加密？→ 不知道

  **完整答案：** 核心原因是性能差异巨大。对称加密（AES-256-GCM）吞吐量可达 GB/s 级别，有 AES-NI 硬件加速；非对称加密（RSA-2048）吞吐量只有几 MB/s，慢 100~1000 倍，且 RSA 一次只能加密 ~245 字节，涉及大整数模幂运算，CPU 开销极高。

  HTTPS 采用混合加密设计扬长避短：握手阶段用非对称加密解决"在不安全信道上协商对称密钥"的难题（客户端生成 pre-master secret 用服务端公钥加密发送，或用 ECDHE 算法协商，具备前向安全性）；通信阶段切换到对称加密传输数据。这是典型的"组合拳"思路，SSH、IPSec、WireGuard 等所有现代加密协议都是如此。

  **延伸 B：** 浏览器是怎么验证一个 HTTPS 证书是可信的？如果中间人伪造了一张证书，浏览器为什么会识别出来？→ 不知道

  **完整答案：** 核心机制是信任链 + 数字签名。

  证书（X.509 格式）包含：域名（CN/SAN）、服务端公钥、颁发者（CA）、有效期、CA 用私钥对以上信息的签名。

  信任链验证流程：根证书（Root CA）→ 中间证书（Intermediate CA）→ 站点证书（Leaf）。浏览器/操作系统预装受信任的根证书（Mozilla CA Bundle 等），根证书是自签名的。验证时逐级向上：用中间 CA 公钥验证站点证书签名，用根 CA 公钥验证中间 CA 签名，根 CA 在信任列表中 → 信任链建立。

  完整验证项（缺一不可）：签名有效、证书未过期、域名匹配、未被吊销（CRL/OCSP/OCSP Stapling）、证书用途正确。

  中间人伪造证书会被识破的原因：
  - 伪造自签名证书：找不到对应根 CA，信任链断裂
  - 修改真实证书：签名哈希不匹配，验证失败
  - 窃取真实证书：没有对应私钥，无法在握手中证明身份
  - 攻破 CA 伪造：现代浏览器有 Certificate Transparency（CT 日志），公开证书必须记录在公共日志中，伪造会被发现并吊销

  工程进阶防御：HSTS（强制 HTTPS，防 SSL Stripping）、Certificate Pinning（已被 Expect-CT 取代）、DNS CAA 记录（限制可签发 CA）。
- 2. HTTP/1.1、HTTP/2、HTTP/3 各有哪些改进？
- 3. HTTP 常见状态码有哪些？分别代表什么含义？
- 4. GET 和 POST 请求的区别是什么？
- 5. HTTP 缓存机制是怎样的？强缓存和协商缓存有什么区别？
- 6. HTTP 请求头和响应头中常见的字段有哪些？
- 7. Cookie、Session、Token 的区别和使用场景？
- 8. 什么是 HTTP 长连接（Keep-Alive）？有什么作用？
- 9. HTTPS 的握手过程是怎样的？TLS 1.2 和 TLS 1.3 有什么区别？
- 10. HTTP 队头阻塞问题是什么？HTTP/2 和 HTTP/3 如何解决？

## TCP/UDP

- 11. TCP 三次握手的过程是什么？为什么需要三次？
- 12. TCP 四次挥手的过程是什么？为什么需要四次？TIME_WAIT 状态的作用？
- 13. TCP 和 UDP 的区别是什么？各自适用什么场景？
- 14. TCP 如何保证可靠传输？
- 15. TCP 的流量控制和拥塞控制是怎么实现的？
- 16. TCP 的粘包和拆包问题是什么？如何解决？
- 17. UDP 如何实现可靠传输？（如 QUIC 协议）

## DNS

- 18. DNS 解析的完整过程是怎样的？
- 19. DNS 使用 UDP 还是 TCP？为什么？
- 20. DNS 劫持是什么？如何防范？
- 21. CDN 的工作原理是什么？

## 浏览器网络

- 22. 从输入 URL 到页面显示的完整过程？
- 23. 什么是跨域？CORS 是如何解决跨域的？
- 24. JSONP 的原理是什么？有什么缺点？
- 25. WebSocket 和 HTTP 长轮询的区别？WebSocket 握手过程？
- 26. 浏览器的同源策略是什么？哪些标签不受同源策略限制？
- 27. 什么是预检请求（Preflight）？什么情况下会触发？
- 28. Service Worker 是什么？如何实现离线缓存？

## 网络安全

- 29. XSS 攻击的原理和防御方式？
- 30. CSRF 攻击的原理和防御方式？
- 31. SQL 注入的原理和防御方式？
- 32. 中间人攻击（MITM）是什么？HTTPS 如何防止？
- 33. 什么是 CSP（内容安全策略）？如何配置？

## 网络模型

- 34. OSI 七层模型和 TCP/IP 四层模型分别是什么？
- 35. IP 地址的分类？IPv4 和 IPv6 的区别？
- 36. 什么是 NAT（网络地址转换）？有什么作用？
- 37. 什么是 ARP 协议？工作原理是什么？
- 38. 什么是 ICMP 协议？ping 命令的原理？
