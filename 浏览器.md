# 浏览器面试题库

## 渲染原理

- 1. 从输入 URL 到页面显示，发生了什么？✅ 2026-05-19 59分

  **答：** 检查URL合法性 → DNS解析（浏览器缓存→OS→hosts→本地DNS→根域名→顶级→权威）→ TCP三次握手 → 发送HTTP报文 → 解析html成DOM树，CSS解析成CSSOM阻塞渲染 → 组成Render Tree → Layout → Paint → Composite多图层合成调用GPU渲染页面 **完整答案：** URL解析检查HSTS缓存 → DNS按浏览器缓存/OS缓存/hosts/本地DNS服务器/根域名/顶级域名/权威域名服务器顺序查找 → TCP三次握手 + TLS握手（HTTPS）→ 检查强缓存，命中直接返回，否则发请求（可能304协商缓存）→ 边下载边解析HTML构建DOM，预加载扫描器并行加载资源，script标签阻塞解析 → 解析CSS构建CSSOM（阻塞渲染不阻塞解析）→ 合并DOM+CSSOM构建Render Tree → Layout计算几何信息 → Paint生成绘制指令 → Composite多图层GPU合成输出屏幕

  **延伸 A：** TCP三次握手的目的是什么？为什么不是两次或四次？ → 不知道 **满分答案：** 三次握手目的是双方互相确认收发能力并同步初始序列号ISN。不能两次：两次无法确认客户端能收到服务端响应，且历史失效SYN会导致服务端建立无效连接浪费资源；不需要四次：第二次握手SYN+ACK可以合并，四次多余。四次挥手需要四次是因为服务端FIN和ACK不能合并，需等数据发完才能发FIN。
- 2. 浏览器的渲染流程是什么？（DOM → CSSOM → Render Tree → Layout → Paint → Composite）
- 3. 什么是回流（Reflow）和重绘（Repaint）？有什么区别？如何优化？
- 4. CSS 和 JS 对渲染的阻塞有什么影响？`async` 和 `defer` 有什么区别？
- 5. 什么是合成层（Composite Layer）？哪些属性会触发合成层？
- 6. requestAnimationFrame 和 setTimeout 做动画有什么区别？

## 事件循环

- 7. 什么是事件循环（Event Loop）？宏任务和微任务有什么区别？
- 8. Promise.then、setTimeout、queueMicrotask、MutationObserver 的执行顺序是什么？
- 9. Node.js 中的事件循环和浏览器有什么不同？process.nextTick 和 Promise.then 谁先执行？

## 缓存机制

- 10. 浏览器缓存机制是什么？强缓存和协商缓存有什么区别？
- 11. Cache-Control 有哪些常用指令？no-cache 和 no-store 有什么区别？
- 12. ETag 和 Last-Modified 有什么区别？为什么 ETag 优先级更高？
- 13. 浏览器缓存的位置有哪些？Service Worker、Memory Cache、Disk Cache 的区别？

## 跨域

- 14. 什么是同源策略？为什么浏览器要有同源策略？
- 15. 跨域有哪些解决方案？各自的原理和适用场景是什么？
- 16. CORS 的简单请求和预检请求（Preflight）有什么区别？
- 17. JSONP 的原理是什么？有什么缺点？

## 浏览器存储

- 18. Cookie、localStorage、sessionStorage、IndexedDB 有什么区别？
- 19. Cookie 的 HttpOnly、Secure、SameSite 属性分别有什么作用？
- 20. Service Worker 是什么？它能做什么？

## 安全

- 21. 什么是 XSS 攻击？有哪些类型？如何防御？
- 22. 什么是 CSRF 攻击？原理是什么？如何防御？
- 23. 什么是 CSP（Content Security Policy）？如何配置？
- 24. 什么是点击劫持（Clickjacking）？如何防御？
- 25. HTTPS 的工作原理是什么？TLS 握手过程是怎样的？

## 性能优化

- 26. 什么是 Core Web Vitals？LCP、FID/INP、CLS 分别代表什么？
- 27. 前端性能优化有哪些手段？从加载和渲染两个维度分别说明。
- 28. 什么是虚拟列表？为什么需要它？如何实现？
- 29. 图片懒加载的实现原理是什么？IntersectionObserver 如何使用？
- 30. HTTP/2 相比 HTTP/1.1 有哪些改进？对前端性能有什么影响？

## 其他

- 31. Web Worker 是什么？和主线程如何通信？有什么使用场景？
- 32. 浏览器的垃圾回收机制是什么？什么是内存泄漏？如何排查？
- 33. 什么是 PWA？Service Worker 在其中扮演什么角色？
- 34. 浏览器的多进程架构是什么？各进程的职责是什么？
- 35. 什么是预加载（preload）和预获取（prefetch）？有什么区别？
